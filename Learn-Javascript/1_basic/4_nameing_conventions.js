/**
 * Naming Conventions
 * 
 * 1) 일반적으로 영어를 사용하며 문자와 숫자를 모두 사용할 수 있다.
 * 2) 특수기호는 언더스코어와 달러를 사용 할 수 있다.
 * 3) 숫자로 이름을 시작 할 수 없다.
 * 4) 키워드는 변수명으로 사용 할 수 없다.
 */
let codefactory = '코드팩토리';
var $ive = '아이브';
const _yuJin = '안유진';

console.log(codefactory);
console.log($ive);
console.log(_yuJin);

// let 1name = 'no';
// let const = 'attention';

/**
 * Naming convention 2
 * 
 * 1) camelCase - 첫 글자는 소문자,두번째는 대문자 - 낙타 모양 같음
 * 2) snake_case - 띄어쓰기 대신 언더스코어로 묶을때 소문자사용
 * 3) PascalCase - C# 마이크로소프트 계열의 언어에서 사용함
 */
//안유진
const anYuJin = '안유진';
console.log(anYuJin);